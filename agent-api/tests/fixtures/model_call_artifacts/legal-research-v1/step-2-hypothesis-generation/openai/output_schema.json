{
  "additionalProperties": false,
  "properties": {
    "hypotheses": {
      "description": "提示されたWorkItemを検証するHypothesis。独立して適用され得る条件、義務又は回答事項ごとに返す。",
      "items": {
        "additionalProperties": false,
        "properties": {
          "gaps": {
            "description": "statementに残る、法令本文で確認すべき具体的な規律要素。該当する要素がなければ空にする。行為者が不明で結論を左右する場合は、その役割の確認も含める。根拠条文の提示や検索作業は書かない。",
            "items": {
              "minLength": 1,
              "type": "string"
            },
            "maxItems": 8,
            "type": "array"
          },
          "statement": {
            "description": "WorkItemへの回答を構成し得る、法令本文の一つの規定内容で個別に支持又は否定できる1つの法的命題。一般的な法的知識を使ってよいが、WorkItemにない行為者、具体的な数値又は条文番号を確定事項として作らない。規定の存在や検索方針ではなく、確認する法的命題を書く。",
            "maxLength": 2000,
            "minLength": 1,
            "type": "string"
          },
          "work_item_id": {
            "enum": [
              "wi-1"
            ],
            "type": "string"
          }
        },
        "required": [
          "work_item_id",
          "statement",
          "gaps"
        ],
        "type": "object"
      },
      "minItems": 1,
      "type": "array"
    }
  },
  "required": [
    "hypotheses"
  ],
  "type": "object"
}
