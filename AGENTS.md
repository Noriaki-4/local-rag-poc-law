# AGENTS.md

このリポジトリで作業するAIコーディングエージェント向けのガイドです。

## 基本方針

- 既存の設計、命名、ディレクトリ構成を優先してください。
- 変更は依頼内容に必要な範囲へ絞り、無関係なリファクタリングは避けてください。
- ユーザーが加えた未コミット変更を勝手に戻さないでください。
- 破壊的な操作や大きな設計変更が必要な場合は、先に意図を確認してください。

## 作業の進め方

- まず `README.md`、`RUNBOOK.md`、設定ファイル、既存コードを確認してから実装してください。
  Docker Compose起動・seed・評価等の操作手順は `RUNBOOK.md` に従ってください。
- ファイル検索には可能な限り `rg` / `rg --files` を使ってください。
- 新しい依存関係を追加する前に、既存の標準ライブラリ、フレームワーク、ヘルパーで実現できないか確認してください。
- 実装後は、可能な範囲でフォーマット、lint、テスト、型チェックを実行してください。

## アプリの試行が期待どおりにならない場合

- まず失敗時の入力と処理境界の入出力を最小fixtureへ固定し、診断モードでPrompt、契約、LLM出力、検証、状態更新を追跡してください。診断情報は必要なときだけ出力します。
- 一括処理を推測で直さず、失敗した処理を単一責務の最小テストへ分けてください。最小構成で成功したら、実際のPrompt、入力、schemaを一要素ずつ戻し、どの境界で失敗するか特定します。
- 原因が分かるまでは契約、validator、修復処理を増やさないでください。先にPromptの役割、手順、入力の意味、判断基準、出力が明確で矛盾していないか確認します。必要ならLLMに作業内容を短く説明させ、理解を照合します。
- 原因箇所だけを修正し、最小fixture、元の失敗fixture、関連する回帰テストの順で確認してください。ID、件数、型、参照整合性はプログラム、意味判断はLLMが担当します。
- 修正を重ねても同じ失敗が続く場合は実装を止め、責務分割と切り分け結果を見直してください。根拠のない場当たり的な変更を続けないでください。

## ドキュメント

- 仕様や操作手順が変わる場合は、関連するドキュメントも更新してください。
- コードコメントは、意図や背景がコードから読み取りにくい箇所に限定してください。

## 注意

- このリポジトリは法務・RAG関連の検証用途を想定しています。
- 法的判断そのものをコードやドキュメントで断定せず、必要に応じて専門家確認が必要であることが伝わる表現にしてください。
