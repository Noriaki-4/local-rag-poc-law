# AGENTS.md

このリポジトリで作業するAIコーディングエージェント向けのガイドです。

## 基本方針

- 既存の設計、命名、ディレクトリ構成を優先してください。
- 変更は依頼内容に必要な範囲へ絞り、無関係なリファクタリングは避けてください。
- ユーザーが加えた未コミット変更を勝手に戻さないでください。
- 破壊的な操作や大きな設計変更が必要な場合は、先に意図を確認してください。

## 作業の進め方

- まず `README.md`、`RUNBOOK.md`、設定ファイル、既存コードを確認してから実装してください。
  Docker Compose起動・seed・評価等の操作手順は `RUNBOOK.md` に従ってください。
- ファイル検索には可能な限り `rg` / `rg --files` を使ってください。
- 新しい依存関係を追加する前に、既存の標準ライブラリ、フレームワーク、ヘルパーで実現できないか確認してください。
- 実装後は、可能な範囲でフォーマット、lint、テスト、型チェックを実行してください。

## アプリの試行が期待どおりにならない場合

- 問題が発生した時点の入力と状態を再現できる最小のfixtureを用意してください。最終出力だけでなく、必要に応じて `AgentView`、Tool結果、LLM入出力、検証結果も固定し、修正後の回帰テストに使ってください。
- 診断モードを使い、原因の特定に必要な範囲でtraceを網羅してください。少なくとも、プログラムからLLMへ渡したPrompt・契約・状態、LLMから返された出力、検証・修復・状態更新の結果を追跡可能にしてください。通常運用で大量の診断情報を常時出力しないでください。
- 契約、validator、修復ロジックを追加・強化する前に、Promptの役割、現在の処理段階、入力の意味、判断基準、期待する出力が明確か、古い指示や矛盾が残っていないかを確認してください。Promptの不備を契約の複雑化で補わないでください。
- LLMがPromptを理解しているか、診断時に確認できるようにしてください。隠れた思考過程の出力には依存せず、現在の作業、入力の意味、判断基準、次に行うことをLLM自身の短い言葉で説明させ、期待する理解と照合してください。
- 修正は、fixtureで再現、traceで原因特定、Promptの修正と理解確認、必要な場合だけ契約を修正、回帰テスト、の順で進めてください。ID、件数、型、参照整合性などの決定的検証はプログラムが担当し、意味判断はLLMに残してください。

## ドキュメント

- 仕様や操作手順が変わる場合は、関連するドキュメントも更新してください。
- コードコメントは、意図や背景がコードから読み取りにくい箇所に限定してください。

## 注意

- このリポジトリは法務・RAG関連の検証用途を想定しています。
- 法的判断そのものをコードやドキュメントで断定せず、必要に応じて専門家確認が必要であることが伝わる表現にしてください。
